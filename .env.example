# Choose "openai" or "gemini" for npm run playground.
EZAGENT_PROVIDER=openai

# Optional: override the playground's default model.
# EZAGENT_MODEL=gpt-4.1-mini

# Optional: reuse a process-local playground session ID.
# EZAGENT_SESSION_ID=playground

OPENAI_API_KEY=
GEMINI_API_KEY=
